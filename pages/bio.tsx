import type { NextPage } from 'next';
import Header from "../components/Header";
import Head from 'next/head';

const Bio: NextPage = () => {
  return (
    
    <div>
      {Header()}
     this is bio page
    </div>
  )
}

export default Bio;